# Keep the build context (and image) small + safe.
.git
.github
.venv
venv
__pycache__
*.pyc
*.pyo

# Local state — must never be baked into the image.
.env
*.db
*.db-wal
*.db-shm
*.log
.coverage
.pytest_cache
.mypy_cache
backups/
screenshots/
.playwright/

# Docs / dev-only
*.md
requirements-dev.txt
tests/
